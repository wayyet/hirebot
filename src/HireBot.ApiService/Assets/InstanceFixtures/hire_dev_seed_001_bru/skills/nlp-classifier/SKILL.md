# nlp-classifier

Purpose: classify incoming requirement text into structured intent labels.

Inputs:

- raw stakeholder message
- business context tags

Outputs:

- intent label
- confidence score
- missing fields hint

