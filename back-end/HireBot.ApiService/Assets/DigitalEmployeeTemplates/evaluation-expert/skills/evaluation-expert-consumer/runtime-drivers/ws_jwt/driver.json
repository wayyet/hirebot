{
  "driver_id": "ws_jwt",
  "version": "2.1.0",
  "protocol": "websocket+jwt",
  "entry": "run.py",
  "language": "python",
  "produces": "runtime-schemas/execution_trace.schema.json",
  "consumes": [
    "runtime-schemas/evaluation_context.schema.json",
    "runtime-schemas/enriched_test_case.schema.json"
  ],
  "capabilities": {
    "supports_multi_turn": true,
    "supports_tool_call_observation": true,
    "supports_auto_approval": true,
    "supports_stdin_control": true
  },
  "config_schema": {
    "type": "object",
    "required": ["endpoint"],
    "additionalProperties": false,
    "properties": {
      "endpoint": {
        "type": "string",
        "minLength": 1,
        "description": "Gateway address. Either bare HOST:PORT, http(s)://..., or ws(s)://..."
      },
      "timeout": {
        "type": "integer",
        "minimum": 1,
        "default": 60,
        "description": "Per-turn assistant_done timeout in seconds (evaluatee side). Bounds how long the driver waits for the evaluatee to finish replying after each 'send' action from the host agent."
      },
      "auto_approve_tools": {
        "type": "boolean",
        "default": true,
        "description": "If true, automatically approves any approval_required messages from the evaluatee."
      }
    }
  },
  "notes": "v2.1: token acquisition is exclusively via evaluation_context.hirebot_api.auth (client_credentials mode). The legacy driver_config.token static-token field has been removed. The host C# must inject OpenSandbox:KingCrab credentials into hirebot_api.auth so the driver can obtain a fresh Keycloak token at startup. Long-lived stdin/stdout JSON protocol unchanged from v2.0."
}
